# The ZX Spectrum tape image: each block's data as the ROM loader would see it,
# with the leader, sync and bit timings left for a loader to reconstruct.
# Graduated from emu198x's own copy once Asm198x needed to *write* what that one
# reads (../../decisions/module-and-crate-naming.md).
[package]
name = "format-sinclair-zx-spectrum-tap"
description = "ZX Spectrum TAP tape images — decode and encode the block stream, including ROM header blocks and parity."
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["zx-spectrum", "sinclair", "tap", "tape", "retro"]
categories = ["encoding", "filesystem"]

[lints]
workspace = true
